Micron
Q6: Circular list

typedef struct linknode

{
    int date;
    struct linknode *next;
}TNode;


// head is the pointer of the head node
// datValues is the data number values of the deleted nodes

TNode * DeleteNode(TNode *head, int deleteValues)
{}
